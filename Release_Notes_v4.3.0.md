# 星TAP 高清缩图 v4.3.0 — 编码器大换血，同尺寸画质更干净、色彩不丢

朋友圈 / 小红书发图被平台压糊？v4.3.0 把底层编码器从 `jpeg-encoder` 换成 **mozjpeg-rs（纯 Rust、零 C 依赖）**，同样是「社交分享 / 高清存档 / 自定义」三档，但**同样的文件大小，画质比 v4.2.0 明显更干净**，而且**原图的 ICC 色彩配置、EXIF、XMP 全部保留，不再丢失**。

## 这次到底提升在哪（都有实测）

| 维度 | v4.2.0 | v4.3.0 | 提升 |
|---|---|---|---|
| 同质量 Q95 不缩放体积 | 9530 KB | 5004 KB | **省 47.5%** |
| 同体积(~2MB) 画质 PSNR | 48.99 dB | 51.60 dB | **+2.6 dB（同大小更清晰）** |
| 同体积(~2MB) 画质 SSIM | 0.9929 | 0.9957 | 更高 |
| 元数据 | 只留首个 APP1（会丢 XMP / 宽色域 ICC） | EXIF + XMP + ICC + MPF 全留 | 零损失 |

### 画质为什么更好了
- **同样的文件大小，码率花在刀刃上**：mozjpeg 的 trellis 量化 + 感知量化表（MssimTuned）会把码率优先分配给人眼敏感的结构和边缘，而不是均匀浪费。所以同样 2MB，v4.3.0 比 v4.2.0 锐利、干净一截。
- **色彩不丢**：很多相机 / 设计稿带 ICC 色彩配置（尤其是广色域 P3 / AdobeRGB）。v4.2.0 只保留第一个 APP1 段，会悄悄丢掉宽色域 ICC，导致色彩发灰发飘。v4.3.0 现在**所有元数据段（EXIF / XMP / ICC / MPF）原样保留**，色彩所见即所得。

### 体积为什么小这么多
- 默认 **4:2:0 色彩子采样**（照片里人眼不敏感的色度降采样，肉眼无感但省约 1/3 码率）+ 渐进式编码 + Huffman 优化。
- 截图 / 文字场景可在「自定义 → 色彩子采样」切到 **4:4:4**（防文字 / 线条模糊）。

## 怎么用（和 v4.2.0 一样）
1. 拖图进窗口 → 自动按「社交分享」压到刚好平台不二压的大小。
2. 想要最高保真：选「高清存档」（不缩放 + 视觉无损）。
3. 想要自己控：选「自定义」调长边 / 质量 / 目标大小 / **色彩子采样**。
4. 命令行 / AI 接口：`--usage-mode` / `--platform` / `--quality-mode` / `--subsampling` 全支持，JSON 信封格式不变。

## 技术细节（给极客）
- 编码器：`mozjpeg-rs` 0.9.2（BSD-3，纯 Rust，无 cmake/nasm/C 依赖）。
- 感知模式量化表：内置 `MssimTuned`（mozjpeg-rs 不支持外部注入自定义表，改用内置感知表，叠加 trellis 画质更优）。
- 全平台纯 Rust 跨平台：macOS / Windows 均原生编译，无外部运行时。
- 向后兼容：不传任何新参数 = v4.1.0 旧行为完全不变；旧版 config.toml 也能正常加载。

## 下载
- `图片高速压缩_Mac_v4.3.0.zip` — macOS .app
- `图片高速压缩_Win_v4.3.0.zip` — Windows exe
