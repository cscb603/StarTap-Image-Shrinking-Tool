# 星TAP 图片高速压缩 — 使用说明 v4.4.4

---

## 这软件干嘛的？

你拍的好照片，用微信发出去就变糊了——不是因为你拍得不好，是微信会自动把大图重新压一遍，画质就丢了。小红书也一样。

**这个工具做的事：在照片发给微信/小红书之前，就帮你按它们的规则提前处理好。** 图的大小不会太大（不会触发它们的二次压缩），但画质顶到了它们能接受的上限——色彩饱满、细节清晰，不再被平台"翻压"变糊。

v4.4.0 新版的核心理念就是：**让平台没法再压你的图，同时把画质天花板顶满。**

---

## v4.4.4 本次更新了什么？（修复 + 补回功能）

**1. 修好了「自定义」模式漏掉的「画质优先」**

上一版（v4.4.3）在「自定义」用途里，画质下拉只有「小而美 / 普通」两档，「画质优先」被弄丢了——你之前反馈的正是这个。现在已补回：

- **「自定义」里画质下拉也是完整三档**：画质优先（推荐）/ 小而美 / 普通，跟社交分享一模一样。
- **进「自定义」默认就是画质优先**：自动套用 Q96 起步 + 4:4:4 全色度 + 自然锐化补偿，不会再把你归成低画质。
- 处理时再兜底：只要选了画质优先，就强制最高画质基线（不限体积），保证输出不缩水。

**2. 软件包把使用说明找回来了**

上一版打包时误把「使用说明」「AI 使用说明（AGENTS.md）」「更新日志」从压缩包里漏掉了，只剩程序和「首次打开必看」。这一版三份文档全部回到压缩包里，下载解压就能看到。

---

## v4.4.3 又升级了什么？（体验 + 修复，历史参考）

**1. 处理完自动帮你打开输出文件夹**

图片压好后，软件会自动弹出输出目录（Finder / 资源管理器），不用再自己去翻。而且做了**去重**：同一目录本轮只弹一次——你点过「📂 打开输出文件夹」按钮，完成时就不会再弹，反之亦然，绝不会连环弹窗烦你。

**2. 主页底部小字，让你看见后台在干嘛**

处理中显示「✨ 正在为你提升画质 · 已处理 N 个」；处理完在底部小字列出这一轮后台自动做的事，比如「已自动：压缩体积 · 微反差锐化 · 微信防二压 · 保留拍摄信息 · 大图防爆…」。不再是黑盒，你知道每一步花在哪了。

**3. 修了一个小 bug**

当你设置了「导出到别的文件夹」时，旧版「打开输出文件夹」按钮会错误地打开原图所在的文件夹。现在会正确打开你设置的目标文件夹。

---

## v4.4.2 升级了什么？（稳定 + 体验，历史参考）

**1. 单张图炸了，整个软件不再跟着崩（P0）**

以前某一张图解码失败（损坏/格式异常），工作线程可能直接 panic，整个界面卡死甚至闪退。现在每张图都在独立保护里处理——**某张失败只标红这一张，其余继续跑**，处理完给你一份"哪些成功、哪些失败"的清单。

**2. 输出目录会自己建好（P0）**

选了"导出到别处"但你手填的那个文件夹还没建？以前会写不进去。现在软件自动帮你把目录建好，不报错。

**3. 哪张图为什么失败，现在看得到了（P2a）**

处理完会多出一个「📋 文件列表」，每张图带状态：✅ 成功 / ❌ 失败 / ⏳ 处理中。**把鼠标悬在失败项上，会显示具体失败原因**（比如"文件不存在或无法解码"），省得你瞎猜哪张出了问题。

**4. 一次拖一两千张也不卡了（P2b）**

以前拖一大堆文件进来，界面会在扫描阶段卡住好几秒。现在扫描搬到了后台线程——拖进去先显示「🔍 正在扫描文件…」，扫完列表才刷新并自动开始处理，主界面全程不卡。

**5. 版本号统一（P1）**

关于页 / 状态栏 / 卡片标题三处的版本显示现在是同一个真源，升版不会再出现"这里显示旧版、那里显示新版"的尴尬。

---

## 跟 v4.4.0 比，多了什么？

**1. 默认就"画质优先"了**

打开就是「画质优先」模式：Q96 起步（接近无损）、颜色完整保留（不是偷工减料的 4:2:0）、锐化补得自然不扎眼。三种用途（社交分享 / 高清存档 / 自定义）都以此为准。

**2. 发微信的画质明显好了**

以前发微信朋友圈的图虽然避免了二压，但本身只有一两百 K 的大小，质感偏软。现在同一个尺寸（1080px），文件可以到 3~8 倍大——多出来的都是画质，不是水份。

**3. 锐化更聪明了——看图下菜，不一刀切**

现在它会自动判断：识别出是人像就少锐一点（皮肤不显假），风景细节多的多补一点，高噪点图直接不锐，原图尺寸和缩放幅度也参与决策。锐化完之后自然不扎眼——像是原图自带的清晰度。

**4. 小红书的图更大更清楚了**

你发小红书竖版，现在默认按它的主流尺寸（1660px 长边）准备——以前只有 1440px。多出来的 220px 就是清晰的差距，而且文件大小更从容（5MB 的安全线，实际只用了几百 K），画质拉满。

**5. 导出目录能记住了（v4.4.1 新增）**

选了「保持原文件名（输出到别处）」和自定义导出目录后，关掉软件再开就恢复默认，得重新点一遍。现在退出时自动记住你选的目录，下次打开直接用。

勾选「保持原文件名（输出到别处）」后，首页会直接显示「导出目录」一行，当前路径一目了然，点「更改」就能换，点「重置」恢复默认。

---

## 日常怎么用？

**最简单：拖拽。** 把照片拖进窗口，它就按「画质优先 + 微信」的默认档直接处理好。输出在 `compressed` 文件夹里。

**想换平台？** 在上方的"平台"里切一下就行：微信 / 小红书 / Instagram / 通用。

**想输出到别的文件夹？** 勾选「保持原文件名（输出到别处）」，然后点「更改」选择你要的文件夹。这个目录会记住，下次不用重新选。

**想对比效果？** 画质-下拉里有三档：
- **画质优先（推荐）**：开箱即用，你大部分时候需要的
- 小而美：想要极致省流量，图小但依然清晰
- 普通：4.1 时代的标准压缩

**拖进去就自动开始了，不需要点"开始"按钮。** 处理完会有完成提示。

**想完全自己掌控参数？** 点「自定义」卡片，展开「自定义参数」面板，自由调长边、质量、目标大小、色彩子采样、导出格式等。

---

## 小贴士

- **不用提前缩图**。无论原图是 2400 万像素还是 6000 万像素，它会自动按平台要求调整尺寸到刚好——不小也不大。
- **处理完的图在哪里**：默认在软件旁边的 `compressed` 文件夹里；如果勾选了「输出到别处」，就在你选的文件夹里。
- **批量也没问题**：一次拖几百上千张图也扛得住，内存不会爆，界面不卡。
- **同名文件不会重复处理**：输出目录里已经有了的就跳过，省时间。
- **某张失败不会中断整批**：失败的那张标红、悬停看原因，其余继续处理。

---

## 下载地址

- 🍎 **Mac 版**：https://wwbfk.lanzoub.com/ifCJB40c6e7e
- 🪟 **Win 版**：https://wwbfk.lanzoub.com/ia0Nm40c6e9g
- GitHub Release（备用）：https://github.com/cscb603/StarTap-Image-Shrinking-Tool/releases/tag/v4.4.4

（Mac 解压即用，Windows 双击即跑；处理全程在本地，不上传任何图片）

有问题随时找小 d 🔧
