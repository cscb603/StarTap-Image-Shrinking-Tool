# 星TAP 高清缩图 v4.4.1 — 导出目录记忆 + 首页目录选择

v4.4.0 把画质拉满了。v4.4.1 回到交互细节：**GUI 里设置的自定义导出目录，现在能记住了；"保持原名（导出到别处）"也不再让人找不着北。**

## 一句话功能

**退出时自动记住你选的导出目录，下次打开直接用；"别处"在哪设，现在一眼就能看到。**

## 这次改了什么

### 1. 导出目录持久化

以前「自定义导出目录」是临时状态，窗口一关就恢复成"默认（原文件旁）"。现在会写进配置文件：

- 选一次目录，以后每次打开都是这个目录
- 想恢复默认点"重置"即可
- 配置存放在系统标准配置目录，卸载时随手删

### 2. 首页直接显示导出目录选择

以前首页只有一个 checkbox「保持原名（导出到别处）」，但"别处"在哪设置得去「自定义参数」面板里找，新用户会懵。

现在：
- 勾选「保持原文件名（输出到别处）」后，**同一区域立刻出现"导出目录"行**
- 当前路径直接显示，旁边就是「更改」「重置」按钮
- 不需要展开高级面板就能改

### 3. 文案更直白

- 「覆盖原图（不改名）」→ **「覆盖原图（直接替换原文件）」**，红色警示更明确
- 「保持原名（导出到别处）」→ **「保持原文件名（输出到别处）」**，语义更清晰

## 兼容性

- v4.4.0 所有功能（画质优先、CAS 锐化、平台预设）全量保持
- 旧版配置文件自动兼容：没有 `custom_output_dir` 字段时默认「原文件旁」
- CLI/JSON 行为不变，AI 调用方不受影响

## 下载

- 🍎 Mac：待上传
- 🪟 Win：待上传
- GitHub Release（备用）：https://github.com/cscb603/StarTap-Image-Shrinking-Tool/releases/tag/v4.4.1
