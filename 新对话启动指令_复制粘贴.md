# v4.2.0 感知压缩实验 — 新对话启动指令（复制下面整段粘贴即可）

---

执行 v4.2.0 感知压缩实验分支，无人值守自动模式（火箭自动驾驶）。

唯一工作目录：`/Users/xtap/Documents/AI/星TAP-高清缩图-v4.2.0-perceptual-exp/`（独立 git 仓，分支 perceptual-exp，基线 commit = v4.1.0 源码，编译缓存已预热）。禁止写入 `星TAP-高清缩图-v4.0.6-纯代码-Mac升级/` 目录（v4.1.0 锁死区）。

第一步：完整读 `.workbuddy/v4.2.0_蓝图.md`（§0–§14 全部），复述铁律清单后立即开工，全程按 **§14 无人值守自动执行协议** 走：不向我提问，所有岔路口按 §14.2 决策表自动决策并记录；每 Phase 过 cargo check + clippy 零警告后独立 commit。

关键前置已完成，不要重做：P0 已验证（jpeg-encoder 0.7 支持 `set_quantization_tables` + `Custom(Box<[u16;64]>)`，还有内置 CustomMsSsim 感知表可先用，详见蓝图 §11.2-6，注意 Custom 表不做 quality 缩放的坑）；测试图在 `test_images/`。

执行顺序：P1 降噪+显著性锐化 → P2-A 内置 CustomMsSsim 接线验证 → P2-B 自算 CSF 感知量化表 → P3 --platform preset + JSON 字段 + metrics + --ab/--benchmark → 验收跑批（baseline/perceptual/ab_output 三组输出）→ 生成 `验收报告_v4.2.0-exp.md` → Mac + Win(cargo xwin) 双平台编译。

停止线（§13）：止于「双平台编译通过 + 验收报告」。不升版本号、不 git push、不打 tag、不 Release、不动外置盘、不动 GUI。盲测对照图留给我回来自己看。跑完后用一段话总结：哪些 Phase 成功、SSIM/体积对比数据、自动决策了什么、有没有遗留问题。

---

## 给星大的备忘（不用粘贴）

- **可选加餐**：中午挂机前，往 `test_images/` 里再丢几张典型照片（人像/风景/暗光/中画幅 TIFF 各来点），验收数据会更有说服力。现在里面已有 GYL_3359/3388 两张单反图，不加也能跑。
- 回来后先看：`验收报告_v4.2.0-exp.md` + `ab_output/` 对照图（放大 200% 看睫毛/暗部）。
- 觉得过线了 → 回主对话说「验收通过，发版」，我拉 rust-master-workflow 走连接器发 4.2.0。
