# 星TAP 图片高速压缩 — Release Notes v4.4.4

**发布日期**：2026-08-03
**类型**：修复 + 打包补回（无破坏性变更）

---

## 🐛 修复

### 1. 自定义模式丢失「画质优先」选项（P0 功能回归）
- **症状**：v4.4.3 在「自定义」用途下，画质下拉只有「小而美 / 普通」两档，「画质优先」不可选；且切到自定义时被强制归一成 `normal`，输出画质跌回标准压缩。
- **根因**：`gui.rs` 自定义分支的下拉框漏写 `max` 选项，并在卡片点击回调里把 `quality_mode` 硬置为 `"normal"`。内核侧 `custom + max` 也无强制基线。
- **修复**：
  - 自定义画质下拉补齐完整三档（画质优先 / 小而美 / 普通），与社交分享一致。
  - 切到「自定义」默认 `quality_mode = "max"`（画质优先），并同步 Q96 / 4:4:4 / CAS 0.35 到高级面板。
  - `app_config_to_process_config` 自定义分支：当选 `max` 时强制最高画质基线（Q≥96、4:4:4、CAS 0.35、不限体积）。
- **验证**：`cargo check --features "gui,cli"` 通过；三用途模式下拉均含画质优先。

### 2. 发布包漏装使用文档（打包回归）
- **症状**：v4.4.3 的 Mac/Win 压缩包只剩 `.app`/`.exe` + `首次打开必看.txt`，丢失了 `使用说明`、`AGENTS.md`（AI 使用说明）、`Release_Notes`。
- **修复**：打包流程显式纳入四份文件——`使用说明_v4.4.4.md`、`AGENTS.md`、`Release_Notes_v4.4.4.md`、`首次打开必看.txt`，并在交付前解压核对内含。

---

## ✅ 不变（相对 v4.4.3）
- 默认启动档仍为「社交分享 + 画质优先」（Q96 + 4:4:4 + CAS）。
- 三用途 / 三画质 / 平台预设 / 高级面板 / 拖入即处理 / 优雅停止 / 输出目录去重自动打开 / 配置持久化 / EXIF 与色彩空间策略全部保留，零功能删减。

---

## 📦 资产
- `图片高速压缩_Mac_v4.4.4.zip`（含 .app + 四份说明文档）
- `图片高速压缩_Win_v4.4.4.zip`（含 .exe + 四份说明文档）
- `图片高速压缩_源码_v4.4.4.zip`（纯净源码）

下载：蓝奏云（国内推荐）Mac · Win ｜ GitHub Release（备用）v4.4.4
