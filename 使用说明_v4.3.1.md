# 星TAP 高清缩图 v4.3.1 — 使用说明

朋友圈 / 小红书发图被平台压糊？本地图片太大传不动？拖进去就压好，画质还比之前更干净——这就是星TAP 高清缩图。

本工具是**同一份文件更小、同尺寸画质更干净**的本地图片压缩器：编码器从 v4.3.0 起换成 mozjpeg（纯 Rust），同样的 2MB，锐利度和色彩都明显更好，而且**原图的拍摄参数（EXIF）、色彩配置（ICC）、修图信息（XMP）全部保留，不再丢失**。

v4.3.1 在 v4.3.0 的基础上，集中修补了"个人批量使用"场景下的几个真实坑（隐藏文件污染结果、输出拍平、透明丢失、不支持格式报错），并把 AI 接口的工程成熟度一起补齐。

---

## 一、怎么打开

### Mac
- 下载 `图片高速压缩.app`，直接双击打开。
- 首次打开若提示「无法验证开发者 / 已损坏」：
  - 在 Finder 里**右键点 app → 打开**（不是双击），点「打开」即可；或
  - 系统设置 → 隐私与安全性 → 看到被拦截的提示点「仍要打开」；或
  - 终端执行 `xattr -d com.apple.quarantine /Applications/图片高速压缩.app`（把路径换成你放的位置）。
- 建议拖进「应用程序」文件夹长期使用。

### Windows
- 下载 `rust_image_compressor.exe`，双击即可运行（零依赖，无需安装任何运行库）。

---

## 二、三步压好（GUI 图形界面）

1. **拖图进去**：把一张或多张照片拖进窗口（也支持拖整个文件夹，会自动递归处理子目录）。
2. **选档位**：
   - **社交分享**（默认）：自动按微信 / 小红书 / Instagram 的卡线压（长边 + 体积双控），发出去还是你的高画质版，不再被平台二次压糊。
   - **高清存档**：原尺寸一像素不动，只压掉冗余体积——照片翻几倍小，细节全留。
   - **自定义**：自己调长边、质量、目标大小，以及**色彩子采样**（见下）。
3. **自动出图**：输出默认在同目录的 `compressed/` 文件夹里，原图不动。

> 界面右上角可切「微信 / 小红书 / Instagram」平台预设；「感知优化」开关默认开（让同样大小更清晰）。

---

## 三、高级选项（自定义档）

- **长边**：限制最长边像素（如 2000），超出的等比缩小。
- **质量**：1–100，越高越清晰、文件越大。
- **目标大小**：想压到某个体积（如 2MB）就填这里，工具自动找合适质量。
- **色彩子采样**（关键！）：
  - **4:2:0（照片默认）**：人眼不敏感的色度降采样，肉眼无感，省约 1/3 码率——普通照片首选。
  - **4:4:4（截图 / 文字）**：不降采样，防止文字、线条、屏幕截图发虚——截图/带字图片切这个。
  - **4:2:2（平衡）**：介于两者之间。
- **输出格式**：JPEG（默认，体积小、兼容最好）/ WebP（更省体积、且支持透明通道）。

---

## 四、AI / 脚本调用（一句话指引）

本工具同时提供命令行与 JSON 接口，方便 AI 助手或自动化脚本调用。常用示例：

```bash
# 压缩单个文件
图片高速压缩 "照片.jpg"

# AI 推荐：直接传 JSON（管道或参数）
图片高速压缩 --json-in '{"files":["照片.jpg"],"quality":85,"max_dim":2000}'

# 只输出 JSON 结果（不弹界面）
图片高速压缩 -i "照片.jpg" --quiet --json

# 预演模式（先试后压，安全）
图片高速压缩 --json-in '{"files":["照片.jpg"],"dry_run":true}'

# 能力探测 / 环境自检
图片高速压缩 --capabilities
图片高速压缩 --self-check

# v4.3.1 新增：保结构输出 + SVG 等原样透传 + 透明填白
图片高速压缩 --json-in '{"files":["相册/"],"preserve_structure":true,"passthrough_unsupported":true,"output_format":"webp"}'
```

完整的 JSON 信封格式、全部参数、流式 JSONL、并发限流、幂等续跑等，见同包内的 **`AGENTS.md`**（给 AI 的说明书）。

---

## 五、常见问题

- **输出在哪？** 默认在原图同目录的 `compressed/`；自定义可指定输出目录。
- **原图会动吗？** 不会，压缩产物另存，`compressed/` 里是新图。
- **批量会崩吗？** 不会，超大图（>80MB / >4000 万像素）自动串行处理，拖一堆也稳。
- **支持哪些格式？** 输入 JPG / PNG / TIFF / WebP 等常见格式；输出默认优化后的 JPEG，可选 WebP（更省体积、支持透明）。SVG 等不支持格式可用 `--passthrough-unsupported` 原样搬运，不报错。
- **同尺寸画质更好是为什么？** mozjpeg 的感知量化表会把码率优先分配给人眼敏感的结构与边缘，而不是均匀浪费；同时 4:2:0 子采样 + 渐进式编码进一步省体积。
- **透明 PNG 压完为什么是白底？** 输出 JPEG 本身不支持透明，v4.3.1 起透明区域自动填白底（不再变黑或丢 alpha）；想要保留透明请选 WebP 输出。

---

## 六、v4.3.1 工程成熟度提升（相对 v4.3.0）

这一版主要解决"个人批量使用"场景下的真实坑，让工具更像一个可信赖的自动化组件：

- **D1 · 系统隐藏文件不再污染结果**：macOS 的 `._xxx.jpg` 资源叉文件以前会被误判为图片、压缩失败并让退出码变成 1（自动化毒药）。现在自动归类为"跳过（skipped）"，退出码保持 0，不会误触发重试。
- **D2 · 输出保结构（可选）**：新增 `--preserve-structure`，把多层级相册原样复刻到输出目录（不再拍平）；`--output-suffix` 可自定义输出文件名后缀（覆盖默认 `_wx/_hd/_da`，空串 = 无后缀）。
- **D3 · 不支持格式原样透传**：新增 `--passthrough-unsupported`，遇到 SVG 等不支持格式时原样复制过去，不压缩、不报错，调用方无需预处理。
- **D4 · 透明 PNG 不再丢透明**：输出为 JPEG（不支持透明）时，透明区域自动填白底，不再变黑或丢 alpha。
- **更多 AI 友好改进**：输出格式新增 WebP（更省体积、支持透明）；FileResult 新增 `error_type`（unsupported / corrupt / permission / skipped / passthrough / error）便于决策重试还是跳过；汇总新增 `skipped` 计数与 `manifest` 输入→输出映射清单。

---

## 七、版本说明

本版 v4.3.1 相对 v4.3.0 的核心提升（均有实测验证）：

| 维度 | v4.3.0 | v4.3.1 |
|---|---|---|
| 系统隐藏文件(._*) | 误报失败、退出码 1 | 归类为 skipped、退出码 0 |
| 多层级目录输出 | 拍平到单目录 | `--preserve-structure` 复刻层级 |
| 不支持格式(SVG 等) | 报失败 | `--passthrough-unsupported` 原样透传 |
| 透明 PNG→JPEG | 丢透明/变黑 | 自动填白底 |
| 输出格式 | JPEG | JPEG + WebP |
| AI 诊断 | 仅 success/error | 细分 error_type + manifest 映射 |

更详细的新增内容与技术细节，见同包内的 **`Release_Notes_v4.3.1.md`**。
