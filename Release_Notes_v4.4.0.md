# 星TAP 高清缩图 v4.4.0 — 防二压画质优先

v4.3.1 把工程坑全填平了。v4.4.0 回到初心：**你拍的好照片，发到微信/小红书就被二次压缩糊掉——现在主动卡住平台甜点阈值，把画质用到天花板，不给平台留乱压的空间。**

## 一句话功能

**防二压画质优先模式**：Q96 起步 · 4:4:4 全色度保留 · CAS 自然锐化补偿 · 体积只作安全线（不主动压小）。微信默认 1080px/900KB 线内全画质保留，小红书 1660px/4.5MB 线内同样拉满。

## 这次加了什么

### 画质优先模式（quality_mode="max"）

三个档位你现在都有：
- **画质优先**（v4.4.0 新增，GUI 默认）：把预算用满，只为防二压
- **小而美感知压缩**（v4.2.0）：同体积画质更好，省流但仍然清晰
- **普通标准压缩**（v4.1.0 旧行为）

画质优先模式下，每个平台都有一套"甜点参数"：

| 平台 | 长边 | 质量起步 | 体积安全线 | 子采样 |
|---|---|---|---|---|
| 微信（全版本） | 1080 | Q96 | 900KB | 4:4:4 |
| 微信新宽幅 iOS | 2560 | Q95 | 2MB | 4:4:4 |
| 小红书 | 1660 | Q96 | 4.5MB | 4:4:4 |
| Instagram | 1080 | Q95 | 1MB | 4:4:4 |
| 通用（中画幅） | 2560 | Q96 | 4.9MB | 4:4:4 |

体积只是安全线——不低于它时不降 Q。图有多大就多大，把预算用满。

### CAS 自然锐化补偿

每次缩放（比如 6000 像素缩到 1080），降采样都会抹掉一层"数学性的柔"。普通 USM 锐化容易出白边/黑边光晕，观感生硬。

CAS（Contrast Adaptive Sharpening）是逐像素内容自适应的：
- **平坦区（天空/皮肤）**：自动跳过，零锐化，不放大噪点
- **边缘纹理**（花蕊/树叶/文字）：自适应增强，不产生光晕
- **强度默认 0.35**（温和自然），仅在缩放比 >1.3 时自动触发

与感知模式的 USM 互斥——感知模式走显著性掩膜 USM 时不触发 CAS，不双重锐化。

### 零依赖，纯 Rust

CAS 手写 ~100 行纯 Rust，3×3 邻域计算。**零新 crate 依赖**，CD 编译不受影响。

## 怎么用

**CLI：**
```bash
# 画质优先（新简写）
rust_image_compressor /path/to/photos --quality-first

# 指定平台 + 画质优先
rust_image_compressor /path/to/photos --quality-first --platform xiaohongshu

# 等价写法
rust_image_compressor /path/to/photos --quality-mode max

# CAS 强度自定义（0=关，0.5=很强，默认 0.35）
rust_image_compressor /path/to/photos --quality-first --cas-strength 0.5
```

**AI / JSON：**
```json
{"files":["/path/to/photo.jpg"], "platform":"wechat", "quality_first":true}
```

**GUI：** 社交分享卡片 → 画质下拉选"画质优先（推荐）"——v4.4.0 默认就是这个。

## 兼容性

- v4.3.1 所有功能（D1–D4、保结构、透传、WebP）全量保持
- GUI 首次升级自动迁移：旧"小而美"档自动切为"画质优先"（可以手动切回去）
- CLI/JSON 保持旧默认行为不变（不传 quality_first = 旧表），AI 调用方不受影响

## 下载

- `图片高速压缩_Mac_v4.4.0.zip` — macOS .app
- `图片高速压缩_Win_v4.4.0.zip` — Windows exe
